# Helios — Docker one-command setup
#
# 1. cp backend/.env.example backend/.env
# 2. Điền MONGO_URI (MongoDB Atlas), JWT_SECRET, và ít nhất một LLM API key
# 3. docker compose up -d --build
#
# Chi tiết biến môi trường: backend/.env.example
